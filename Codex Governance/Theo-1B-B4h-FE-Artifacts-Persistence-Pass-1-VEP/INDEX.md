# Codex Governance Package — Theo 1B B4h-FE Artifacts Persistence Pass-1 VEP

- **Main artifact:** `Theo_1B_B4h_FE_Artifacts_Persistence_VEP.md` — Pass-1 Frontend VEP (plan only). Reviewer = Codex (Pass 2). FE-VEP shape: GCR + Rule Anchor Table + F-P1…F-P7 + CCT + Mechanical lint block.
- **Microstep:** Tier **B4h-FE** — wire the deployed B4h artifact endpoints so artifacts persist (Walter: "let's knock out artifacts persistence"; "the artifacts section doesn't seem to work"). Three parts:
  1. **Persist-on-ingest** — `send` best-effort `theo_upsert_artifact` per `[[ARTIFACT]]` block (create-or-add-version by title), tagged with the conversation; refresh gallery.
  2. **Cross-chat gallery** — Artifacts tab loads `theo_list_artifacts` summaries; click → `theo_get_artifact` (versions + content from Blob) → panel.
  3. **Reload re-derive** — `selectRecent` rebuilds a reopened thread's artifacts by re-running the ingest pipeline over each persisted assistant turn (raw `[[ARTIFACT]]` blocks are retained in `theo_messages.content` — **verified this turn by read-only SQL**, 13/71 assistant turns).
- **Backend: already deployed** (B4h `9c9c4d8`); API Spec §2.3 landed (`db79681`). No backend change, no migration.
- **Proposed source (`proposed-src/theo/`)** — 10 modifications: `types.ts` (`ArtifactSummary`), `services/theoClient.ts` (`ingestReply` returns blocks; `resetArtifacts`/`mergeArtifact`; 3 passthroughs), `services/gateway.live.ts` (3 live calls + mappers), `services/gateway.mock.ts` (stubs), `useTheoState.ts` (gallery state + loaders + persist-on-ingest + reload re-derive + open-from-gallery), `components/ArtifactsView.tsx` (render summaries), `components/TheoMain.tsx` (gallery props), `TheoSurface.tsx` (load on mount), plus the **Codex round-1 fix**: `components/CitedText.tsx` (+`renderText` render-prop) + `components/ChatView.tsx` (pass `renderAssistant` to `CitedText`).
- **Codex Pass-2 REJECT round 1 → fixed:** a cited assistant turn that also emitted an artifact fed the `\0A:<id>\0` placeholder into `CitedText`→`Formatted` (no `splitAssistant`), so the card wouldn't render. Fix: `CitedText` takes an optional `renderText` render-prop (defaults to `Formatted` — plain cited answers unchanged) and `ChatView` passes `renderAssistant`, so cited-and-artifact turns render both the card and the citation chips.
- **Classification:** VISUAL-AUTHORITY-DEVIATION (minor — gallery renders summaries not content-preview; Walter-authorized). In-thread cards + panel unchanged (MATCH); `CitedText` change is an additive render-prop (default preserves VA-T5 exactly).
- **Validation this turn:** applied to a scratch copy of `src` → `tsc` (exit 0) + `eslint .` (exit 0) + `build` green (TheoSurface 237.03 KB / 70.46 KB gzip); `src` reverted (package carries only `proposed-src/`). Microstep lint → PASS.
- **Currency:** vault-theo HEAD `db79681`.
- **Pipeline:** Author = Claude Code (Pass 1). Reviewer = Codex (Pass 2). On APPROVAL → Pass 3 apply to `development` + Walter redeploys the Theo SWA. **Completes the artifacts-persistence tier (B4h backend + spec + FE).**
