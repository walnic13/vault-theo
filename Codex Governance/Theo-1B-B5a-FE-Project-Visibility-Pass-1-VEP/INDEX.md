# Codex Governance Package — Theo 1B B5a-FE Project Visibility Pass-1 VEP

- **Main artifact:** `Theo_1B_B5a_FE_Project_Visibility_VEP.md` — Pass-1 Frontend VEP (plan only). Reviewer = Codex (Pass 2). FE-VEP shape: GCR + Rule Anchor Table + F-P1…F-P7 + CCT + Mechanical lint block.
- **Microstep:** Tier **B5 Phase 1 FE** — the sharing UI over the deployed B5a backend: (1) a Private↔"Shared with the team" **toggle** on the project home (owner-only); (2) **"Shared" / "Shared with you" badges** on the Projects grid; (3) **ownership-gated editing** — a shared-with-me project is read-only + chattable (member reads knowledge/instructions, starts their own chats, can't edit/share/rename/delete).
- **Backend: already deployed** (B5a `bfde0cd`); §2.2 landed (`0560ea8`). No backend change.
- **Proposed source (`proposed-src/theo/`)** — 9 modifications: `types.ts` (`Project` +`visibility`/`isOwner`), `data.ts` (seed type relaxed), `services/gateway.live.ts` (+`setProjectVisibility`, map fields), `services/gateway.mock.ts` (+fields, +`setProjectVisibility`), `services/theoClient.ts` (passthrough), `useTheoState.ts` (`setProjectVisibility` optimistic+rollback), `components/ProjectsView.tsx` (badge + owner-gated actions), `components/ProjectDetail.tsx` (toggle + ownership-gated edits + member read-only), `components/TheoMain.tsx` (prop).
- **Classification:** VISUAL-AUTHORITY-DEVIATION (Walter-authorized sharing affordances). Config-only sharing — chat transcripts stay private (server-enforced).
- **Codex Pass-2 REJECT round 1 → fixed:** the sharing toggle could issue overlapping/out-of-order visibility writes. Added a per-project in-flight guard (`visReq` ref) + disabled toggle (`visPending`→`visibilityBusy`, shows "Updating…") in useTheoState/ProjectDetail/TheoMain — serialized per project, no stale state.
- **Validation this turn:** applied to a scratch copy of `src` → `tsc` (exit 0) + `eslint .` (exit 0) + `build` green (TheoSurface 240.53 KB / 71.25 KB gzip); `src` reverted (package carries only `proposed-src/`). Microstep lint → PASS.
- **Currency:** vault-theo HEAD `0560ea8`.
- **Pipeline:** Author = Claude Code (Pass 1). Reviewer = Codex (Pass 2). On APPROVAL → Pass 3 apply to `development` + Walter redeploys the Theo SWA. **Completes Phase 1 of sharing/visibility.** Phase 2 (roster/presence + per-member invite) is separate (needs Graph admin-consent).
