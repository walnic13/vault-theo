# Codex Governance Package — Theo 1B Cold-open Splash Full-Viewport Parity FE Pass-1 VEP

- **Main artifact:** `Theo_1B_ColdOpen_SplashFullscreen_FE_VEP.md` — Pass-1 frontend VEP (plan). Reviewer = Codex (Pass 2). Open it with the governance-bound GCR + Rule Anchor Table (Theo FE Conformance §3/§5).
- **Microstep:** the landed cold-open `RestoringSplash` uses `position: absolute; inset: 0` inside `ChatView`, so on mobile it's clipped to the ChatView box — a thin top strip (app top bar / safe-area) stays uncovered and the fixed-112px spiral looks smaller than the OS/Origin PWA boot splash (Walter: "thin white strip at the top … Origin splash covers the entire screen, logo ~30% larger"). Make it **full-viewport**: portal to `document.body` + `position: fixed; inset: 0` + high z-index (covers the whole screen), and size the spiral to the manifest's on-screen proportion (~36% of viewport width, capped).
- **Changed (1 file, staged in `proposed-src/theo/components/`):** `ChatView.tsx` — `import { createPortal }`; `RestoringSplash` → `createPortal(fixed inset:0 full-screen, document.body)` with a `typeof document` guard; spiral `112` → `min(round(innerWidth × 0.36), 200)`. No prop/state/backend/contract/schema change; `SpiralMark`/`useTheoState`/`TheoMain` unchanged.
- **Visual classification:** splash = **VISUAL-AUTHORITY-DEVIATION** (now full-viewport parity with the deployed Origin boot splash) anchored Golden Pack §5; steady-state chat surface = **VISUAL-AUTHORITY-MATCH**. Only registered VA-T1 cited (T21-safe). **No `localStorage`/`sessionStorage`** (Governor §6.3). Walter SWA (open app → splash covers whole screen, logo matches, → last chat) = Visual Acceptance Evidence.
- **Contract:** none — purely presentational. No backend/API/schema change.
- **Validation:** file applied to `src` this turn → `tsc --noEmit` (exit 0) + `eslint` (exit 0, **no warnings** on `ChatView.tsx`) + `vite build` (exit 0; TheoSurface 306.96 kB / 90.46 kB gzip); `src` reverted. Microstep lint → PASS. Proposed-src blob `ChatView.tsx`=`8d51b0e3`.
- **Pipeline:** Author = Claude Code (Pass 1). Reviewer = Codex (Pass 2). On APPROVAL → Pass 3 commits the 1 file to `development`; the Theo dev SWA serves it.
