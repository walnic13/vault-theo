# Codex Governance Package — Theo 1B FindImage Gallery Thumbnail Full-Image FE Pass-1 VEP

- **Main artifact:** `Theo_1B_Gallery_Thumbnail_FullImage_FE_VEP.md` — Pass-1 frontend VEP (plan). Reviewer = Codex (Pass 2). Open it with the governance-bound GCR + Rule Anchor Table (Theo FE Conformance §3/§5).
- **Microstep:** FindImage gallery thumbnails currently cover-crop each photo into a fixed 150px band (`objectFit: "cover"`), so the whole image is never shown (Walter: thumbnails should "resize the image so they fit"). Switch the thumbnails to **natural height** (`height: "auto"`, no crop) so each image renders whole at its true aspect ratio, and align the grid to the top (`alignItems: "start"`) so variable-height images sit cleanly.
- **Changed (1 file, staged in `proposed-src/theo/components/`):** `ChatView.tsx` — **2 inline-style lines** in the gallery render (grid `alignItems: "start"`; `multi` thumbnail `height:150 + objectFit:cover` → `height:"auto"`) **+ 1 stale-comment fix** (the `ImageLightbox` header comment no longer calls the thumbnails "cover-cropped"). No component/prop/state/backend/contract/schema change.
- **Visual classification:** the thumbnail re-size = **VISUAL-AUTHORITY-DEVIATION** (visual change to the rendered surface) anchored to Golden Pack §5; rest of the chat surface = **VISUAL-AUTHORITY-MATCH**. Only registered VA-T1 cited — T21-safe. Walter SWA screenshot = Visual Acceptance Evidence. The tap-to-expand viewer (landed) is unchanged.
- **Contract:** none — renders the existing `InlineImageItem` shape. No backend/API/schema change.
- **Validation:** file applied to `src` this turn → `tsc --noEmit` (exit 0) + `eslint` (exit 0, **no warnings** on `ChatView.tsx`) + `vite build` (exit 0; TheoSurface 299.40 kB / 87.03 kB gzip); `src` reverted. Microstep lint → PASS. Proposed-src content-addressed blob `ChatView.tsx`=`35b07ab` (HEAD-independent; the reviewed-commit SHA is carried in the submission note).
- **Pipeline:** Author = Claude Code (Pass 1). Reviewer = Codex (Pass 2). On APPROVAL → Pass 3 commits the 1 file to `development`; the Theo dev SWA serves it.
