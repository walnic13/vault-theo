# Theo 1B — VC-2d `theo_chat_typing` API Spec — Role-C Verbatim-Edit Handoff (index)

**Regime:** Theo backend. **Type:** Role-C Verbatim-Edit Handoff (Governor §11) — Codex executes inline (Pass 4). **Lint:** PASS.

## Purpose
Documents the deployed + golden-verified `theo_chat_typing` (VC-2d) in Theo API Spec §2.10 — one appended row. Ephemeral endpoint → **no Schema change** (no Schema-doc edit). Closes the VC-2d API-Spec Gap Register future-trigger.

## Contents
| File | Role |
| ---- | ---- |
| `Theo_1B_VC2d_API_Spec_Typing_RoleC.md` | GCR + Rule Anchor Table + 1 verbatim EDIT (append the typing row after the §2.10 `mark read` row). |
