# Codex Governance Package — Theo-in-Origin Mount Model (Role-C Doc Update)

**Pipeline:** Vault Theo frontend · Pass 4 Role-C · Author = Claude Code · Inline executor = Codex · Authority = Walter
**Purpose:** Capture Walter's Theo-in-Origin mount intent (2026-06-25) into the Theo authority docs so Pass B re-authors against current authority.
**vault-theo HEAD:** `308a05a05d656a25b363d934d5c47680e542e764` (`development`)
**Status:** Authored — awaiting Walter authorization + Codex inline execution. No docs edited yet.

## Contents
- `Theo_Mount_Model_Role_C_Handoff.md` — GCR + Rule Anchor Table + 4 verbatim before/after edits.

## The 5 edits
1. **VA-T2** (`THEO_ARCHITECTURE_AND_STRUCTURE.md`) — insert new **§3A Origin Host & Mount Model** (landing 9/10, permanent 1/10 nav section, app-sidebar stacking, in-app right-hand panel, in-shell Module Federation / no iframe).
2. **VA-T3** (`THEO_1A_FRONTEND_HANDOVER.md`) — replace **§4** with the specific additive Origin mount spec.
3. **Plan** (`THEO_PHASE_1A_FRONTEND_PLAN.md`) — rewrite the **§3 Pass B** row to the mount + app-context scope.
4. **Plan** §5 — add acceptance criterion **#8** (Theo mounted in the Origin shell) + update the pass-mapping line.
5. **Conformance** (`THEO_FRONTEND_GROUNDING_CONFORMANCE_STANDARD.md`) — add **VA-T4** §4B registry row for the Theo-in-Origin layout.

## Flagged for Walter
- **Companion vault-origin App Host Contract update** (separate repo/regime) — admit a persistent assistant-panel mount + register the additive `AppHostContext` app-context accessor; land before/with Pass B Increment B-ii.

## Downstream
- The existing `Theo-1A-Pass-B-App-Context-Layer-Pass-1-VEP` package is now **mis-scoped** against this topology and will be **re-authored** after these edits land.
